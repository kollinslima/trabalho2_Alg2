#include<stdio.h>
#include<assert.h>
#include"btree.h"

int split(FILE *fi, tPage *page, int RRN, tKey key, tKey *propo_key, int *propo_r_child){
    
    int total_keys = N_KEYS;
    int total_ordem = ORDER;
    
    //Fazer sort da lista considerando elemento de overflow antes de começar.
    
    return 0;

}